# SibirskySpeak improvement masterlist

**Merged into `docs/PATCH_NOTES.md`.** This file and `PATCH_NOTES.md` had drifted
into describing the same v2.0.0 audit/implementation pass from two angles
(checklist vs. changelog) with near-total overlap. `PATCH_NOTES.md` is now the
single source of truth for what shipped in that pass, what was verified, and
what remains as deferred/known-limitation work — see its "Known limitations
and deferred work" section for the items this file used to track as unchecked
(`[ ]`) entries.

Kept as a redirect stub rather than deleted, in case anything still links here
by path.
